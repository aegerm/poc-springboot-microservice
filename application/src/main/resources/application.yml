spring:
  datasource:
    url: jdbc:mysql://localhost:3309/db_system
    username: aegerm
    password: sysdb
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    database-platform: org.hibernate.dialect.MySQL8InnoDBDialect
    generate-ddl: true